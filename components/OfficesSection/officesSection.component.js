import officesJSON from '../../assets/locale/countries/offices_es_ES.json'

export default {
    props: {
        officeGroups: {
            type: Array,
            required: true
        }
    },
    setup(props) {
    }
}
